apply plugin: 'com.android.application'

android {
    compileSdkVersion 27
    buildToolsVersion '27.0.3'

    defaultConfig {
        minSdkVersion 17
        targetSdkVersion 27
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    lintOptions {
        abortOnError false
    }

}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')

    implementation "com.android.support:appcompat-v7:$androidLib"

    // ReactiveX
    implementation "io.reactivex:rxjava:1.3.8"

    //api "com.trayis:simpliMVP:1.0.5"
    //implementation "com.trayis:simpliMVPAnnotationProcessor:0.2.0"
    //annotationProcessor "com.trayis:simpliMVPAnnotationProcessor:0.2.0"

    implementation project(":simpliMVP")
    implementation project(":simpliMVPAnnotationProcessor")
    annotationProcessor project(":simpliMVPAnnotationProcessor")

    androidTestImplementation "com.android.support.test.espresso:espresso-core:3.0.1"

    testImplementation "junit:junit:4.12"
    testAnnotationProcessor "com.trayis:simpliMVPAnnotationProcessor:0.2.0"
    // testAnnotationProcessor project(":simpliMVPAnnotationProcessor")

}

